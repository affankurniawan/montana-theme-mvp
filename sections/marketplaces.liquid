<section class="section section--spacious surface" style="background:var(--color-surface); padding-top: 64px; padding-bottom: 64px;">
  <div class="page-width">
    {%- render 'section-heading', eyebrow: section.settings.eyebrow, title: section.settings.heading, text: section.settings.text -%}
    <div class="grid grid--4" style="gap:24px;">
      {%- for block in section.blocks -%}
        <div class="icon-card" style="text-align:center; padding:24px; display:flex; flex-direction:column; align-items:center; justify-content:center; cursor:pointer;" onclick="window.open('{{ block.settings.link }}', '_blank')" {{ block.shopify_attributes }}>
          {%- assign fallback_img = '' -%}
          {%- if block.settings.title contains 'Shopee' -%}{%- assign fallback_img = 'logo-shopee.png' -%}
          {%- elsif block.settings.title contains 'Tokopedia' -%}{%- assign fallback_img = 'logo-tokopedia.png' -%}
          {%- elsif block.settings.title contains 'Lazada' -%}{%- assign fallback_img = 'logo-lazada.png' -%}
          {%- elsif block.settings.title contains 'TikTok' -%}{%- assign fallback_img = 'logo-tiktok.png' -%}
          {%- endif -%}
          
          {%- if block.settings.image != blank -%}
            <img src="{{ block.settings.image | image_url: width: 160 }}" alt="{{ block.settings.title }}" style="height:64px; width:auto; margin-bottom:16px; object-fit:contain;" loading="lazy">
          {%- elsif fallback_img != '' -%}
            <img src="{{ fallback_img | asset_url }}" alt="{{ block.settings.title }}" style="height:64px; width:auto; margin-bottom:16px; object-fit:contain;" loading="lazy">
          {%- endif -%}
          <h3 style="margin:0; font-size:1.25rem;">{{ block.settings.title }}</h3>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Marketplaces",
  "settings": [
    {"type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Official Stores"},
    {"type": "text", "id": "heading", "label": "Heading", "default": "Shop on your favorite marketplace"},
    {"type": "textarea", "id": "text", "label": "Text", "default": "We are available on all major platforms for your convenience and secure transactions."}
  ],
  "blocks": [
    {
      "type": "marketplace",
      "name": "Marketplace",
      "settings": [
        {"type": "text", "id": "title", "label": "Marketplace Name"},
        {"type": "url", "id": "link", "label": "Link"},
        {"type": "image_picker", "id": "image", "label": "Logo"}
      ]
    }
  ],
  "presets": [{
    "name": "Marketplaces",
    "blocks": [
      {"type": "marketplace", "settings": {"title": "Shopee", "link": "https://shopee.co.id/"}},
      {"type": "marketplace", "settings": {"title": "Tokopedia", "link": "https://www.tokopedia.com/"}},
      {"type": "marketplace", "settings": {"title": "Lazada", "link": "https://www.lazada.co.id/"}},
      {"type": "marketplace", "settings": {"title": "TikTok Shop", "link": "https://www.tiktok.com/"}}
    ]
  }]
}
{% endschema %}
